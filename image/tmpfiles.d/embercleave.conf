# /run is tmpfs, recreated each boot. systemd-tmpfiles makes the swarm-owned
# bus socket directory exist before embercleave-mgr.service starts.
# Mode 0750: swarm rwx, group r-x (room for a future bus-reader group),
# others none.
d /run/embercleave 0750 swarm swarm -
